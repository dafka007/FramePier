# These errors are deliberately complete sentences shown directly in the UI.
# Keep all default correctness checks; do not rewrite user-facing copy to
# satisfy the convention for errors intended only for Go callers.
checks = ["inherit", "-ST1005"]
